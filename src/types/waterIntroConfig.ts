export interface WaterIntroConfig {
	enable: boolean;
	playOncePerSession: boolean;
	sessionStorageKey: string;
	startDelayMs: number;
	durationMs: number;
	zIndex: number;
	waterColor: string;
	skipButtonLabel: string;
}
