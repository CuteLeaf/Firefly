import type { PageDisplayConfig } from "@/types/pageDisplayConfig";

export const pageDisplayConfig: PageDisplayConfig = {
	// 文章页显示配置

	// 是否显示文章页的分享按钮
	share: true,
	// 是否显示上一篇/下一篇文章导航
	postNavigation: true,
	// 是否显示相关文章推荐
	relatedPosts: true,
	// 是否显示随机文章推荐
	randomPosts: true,
};
