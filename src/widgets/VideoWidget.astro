---
const { widget } = Astro.props;
const placeholder = widget?.aposPlaceholder ? "true" : "";
---

<style>
	.video-widget {
		width: 100%;
	}
</style>

{widget?.video?.html && <div class="video-widget" set:html={widget.video.html} />}
