---
import { backgroundWallpaper, siteConfig } from "@/config";

const homeText = backgroundWallpaper.banner?.homeText;
const title = homeText?.title || siteConfig.title;
---

<style>
	.home-hero {
		padding: 0;
	}
</style>
